name: CI/CD


on:
  push:
    branches:
      - master
      - release
  pull_request:
    branches:
      - master


jobs:
  lint:
    uses: ./.github/workflows/linting.yaml

  test:
    needs: lint
    uses: ./.github/workflows/testing.yaml
